root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/gdrive-sync ."
  bin = "./tmp/gdrive-sync"
  include_ext = ["go"]
  exclude_dir = ["tmp", "frontend", "node_modules", ".git"]
  delay = 1000

[misc]
  clean_on_exit = true
